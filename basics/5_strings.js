const name = "satya";
let age = 24;

console.log(name+"  "+age);


console.log(name + " " + age);

// Modern way: use backticks (string interpolation)
console.log(`Hello, my name is ${name} and my age is ${age}`);
/**
 * 
 * charAt(index): Returns the character at the specified index.
 * 
charCodeAt(index): Returns the Unicode of the character at the specified index.

concat(str1, str2, ...): Joins two or more strings.

endsWith(searchString, length): Determines whether a string ends with the characters of a specified string.

includes(searchString, position): Determines whether a string contains the characters of a specified string.

indexOf(searchValue, fromIndex): Returns the position of the first occurrence of a specified value.

lastIndexOf(searchValue, fromIndex): Returns the position of the last occurrence of a specified value.

localeCompare(compareString): Compares two strings in the current locale.

match(regexp): Searches a string for a match against a regular expression and returns the matches.

matchAll(regexp): Returns an iterator of all matches in a string against a regular expression.

normalize(form): Returns the Unicode Normalization Form of the string.

padEnd(targetLength, padString): Pads the current string from the end with a given string.

padStart(targetLength, padString): Pads the current string from the start with a given string.

repeat(count): Returns a new string with a specified number of copies of an existing string.

replace(searchValue, newValue): Replaces occurrences of a substring with a new substring.

replaceAll(searchValue, newValue): Replaces all occurrences of a substring with a new substring.

search(regexp): Searches a string for a match against a regular expression and returns the position.

slice(startIndex, endIndex): Extracts a part of a string and returns it as a new string.

split(separator, limit): Splits a string into an array of substrings.

startsWith(searchString, position): Determines whether a string starts with the characters of a specified string.

substring(startIndex, endIndex): Returns the part of the string between the start and end indexes.

toLocaleLowerCase(): Converts the string to lowercase according to the host's locale.
toLocaleUpperCase(): Converts the string to uppercase according to the host's locale.
toLowerCase(): Converts the string to lowercase.
toString(): Returns the value of a String object.
toUpperCase(): Converts the string to uppercase.
trim(): Removes whitespace from both ends of a string.
trimEnd(): Removes whitespace from the end of a string.
trimStart(): Removes whitespace from the beginning of a string.
valueOf(): Returns the primitive value of a String object.
 * 
 */

